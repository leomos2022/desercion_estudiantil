# Este directorio contiene los modelos entrenados

Los archivos que se generan aquí:
- `desercion_model.joblib` - Modelo de predicción de deserción
- `scaler.joblib` - Escalador de features

Estos archivos se generan ejecutando:
```bash
python train_model.py
```
